# --- PostgreSQL (contenedor tramita-postgres, ya corriendo) ---
DB_URL=
DB_USER=
DB_PASSWORD=

# --- Cuenta semilla de la Coordinación (D8: el seeder la crea al arrancar, hash BCrypt) ---
SEED_COORD_EMAIL=coordinacion.cali@uniremington.edu.co
SEED_COORD_PASSWORD=<tu-frase-de-paso-real-de-15+-caracteres>

# --- Allowlist CORS del SPA (coma-separada, sin comodín) ---
APP_CORS_ALLOWED_ORIGINS=